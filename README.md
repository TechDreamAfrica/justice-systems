# TaskFlow — Firebase SaaS Starter

A production-ready task management dashboard built with HTML5, Tailwind CSS,
Vanilla JavaScript (ES6+), and Firebase v10+.

---

## 📁 Project Structure

```
taskflow/
├── index.html          # Single-page app shell + all views
├── styles.css          # Custom CSS (animations, tokens, dark mode)
├── app.js              # All app logic (auth, Firestore, UI, CRUD)
├── firebase-config.js  # Firebase project credentials + SDK init
└── README.md
```

---

## ⚡ Quick Start

### 1. Create a Firebase project

1. Go to https://console.firebase.google.com
2. Click **Add project** → follow the prompts.
3. In the left sidebar, click **Authentication** → **Get started** →
   enable **Email/Password**.
4. In the left sidebar, click **Firestore Database** → **Create database**
   → choose a region → start in **Production mode**.

### 2. Get your credentials

1. In the Firebase Console, click ⚙️ **Project Settings**.
2. Scroll to **Your apps** → click **</>** (Web) → register the app.
3. Copy the `firebaseConfig` object.

### 3. Paste credentials into firebase-config.js

Open `firebase-config.js` and replace the placeholder values:

```js
const firebaseConfig = {
  apiKey:            "AIzaSy...",
  authDomain:        "my-project.firebaseapp.com",
  projectId:         "my-project",
  storageBucket:     "my-project.appspot.com",
  messagingSenderId: "123456789",
  appId:             "1:123456789:web:abcdef"
};
```

### 4. Run locally

Because `app.js` uses ES modules (`import`), you need a local HTTP server.

**Option A — VS Code Live Server** (easiest)
- Install the Live Server extension → right-click `index.html` → Open with Live Server.

**Option B — Node.js**
```bash
npx serve .
# or
npx http-server .
```

**Option C — Python**
```bash
python -m http.server 8080
```

Open http://localhost:8080 in your browser.

---

## 🔒 Firestore Security Rules

Paste these rules in **Firestore Database → Rules** in the Firebase Console.
They ensure every user can only read and write their own tasks.

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Users can only access their own tasks.
    match /tasks/{taskId} {
      allow read, update, delete: if request.auth != null
                                  && resource.data.uid == request.auth.uid;

      allow create: if request.auth != null
                    && request.resource.data.uid == request.auth.uid
                    && request.resource.data.title is string
                    && request.resource.data.title.size() > 0
                    && request.resource.data.title.size() <= 120;
    }

    // Deny everything else by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

Click **Publish** after pasting.

---

## 🌐 Deploy to Firebase Hosting

### 1. Install Firebase CLI

```bash
npm install -g firebase-tools
```

### 2. Log in

```bash
firebase login
```

### 3. Initialise Hosting in your project folder

```bash
firebase init hosting
```

Prompts:
- **Which Firebase project?** → select the project you created.
- **Public directory?** → `.` (a single dot — serve from the root).
- **Configure as a SPA?** → `No` (static files only).
- **Overwrite index.html?** → `No`.

### 4. Deploy

```bash
firebase deploy --only hosting
```

Your app will be live at `https://YOUR_PROJECT.web.app`.

---

## ✨ Features

| Feature | Details |
|---|---|
| Auth | Email/password sign-up & login, session persistence, password strength meter |
| CRUD | Create, read, update, delete tasks with Firestore |
| Real-time | `onSnapshot()` keeps the UI in sync instantly |
| Dashboard | Stats cards, completion progress bar, recent tasks |
| My Tasks | Filterable by status, full-text search, inline edit/delete |
| Profile | Update display name, change password |
| Dark mode | System-aware toggle, persisted in localStorage |
| Responsive | Mobile-first, collapsible sidebar on small screens |
| Notifications | Animated toast system (success / error / info) |

---

## 🛠 Customisation tips

- **Brand colours** — change `--brand-500` / `--accent` in `styles.css` `:root`.
- **App name** — find & replace "TaskFlow" in `index.html`.
- **Add fields** — extend the modal form in `index.html` and the `payload`
  object in `handleTaskSubmit()` in `app.js`.
- **Google Sign-In** — import `GoogleAuthProvider` + `signInWithPopup` from
  Firebase Auth and add a button to the auth cards.

---

## 📝 Recommended .firebaserc (auto-generated by CLI)

```json
{
  "projects": {
    "default": "YOUR_PROJECT_ID"
  }
}
```

## 📝 Recommended firebase.json (auto-generated by CLI)

```json
{
  "hosting": {
    "public": ".",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**",
      "README.md"
    ]
  }
}
```
